spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yourusername/your-config-repo
          clone-on-start: true

server:
  port: 8888

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
